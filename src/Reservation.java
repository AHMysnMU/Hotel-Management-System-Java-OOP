public interface Reservation {
    // class that oblige the definition of the following methods

     void bookroom (int guestID, int roomID);

     void cancelBooking (int guestID);

     void checkIn (int guestID);

}
